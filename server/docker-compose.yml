version: '3.8'

services:
  canarytalk-server:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - JWT_SECRET=${JWT_SECRET:-change-this-secret-key}
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
    restart: unless-stopped
